# Frozen programme transfer: execution details

26 September 2026, after discovery and before any V1 programme scores. Discovery
selected 50 of 134 qualifying genes; commit `f1a1df7` freezes the instrument.
The original configuration and selection criteria remain unchanged.

Apply average tied ranks divided by the 11,590-gene universe size within each
cell, then average the 50 gene ranks. Average cells within each unit/state;
calculate paired intermediate-minus-endpoint differences and report every unit.
For each endpoint, report the median, positive-unit fraction and every
leave-one-unit-out median. V1 must pass the already specified rule for both
endpoints. Discovery-context score effects are descriptive selected estimates.
There is no cross-assay z-score pooling and no confirmatory p-value at three mice.

D1's descriptive equal-day sensitivity is the arithmetic mean of the within-day
mean paired differences. Also report the median after omitting each entire day.
These diagnostics do not replace the primary equal-unit median decision.

Specificity testing is conditional on positive primary V1 transfer. If either
endpoint fails, stop the proposed transferable-programme claim and omit further
stress/cycle exclusion, random-module and depth-rescoring work: there is no
positive transferred association whose specificity needs establishing. Do not
use a secondary score or altered gene set to rescue that failed primary result.
If transfer passes, execute the frozen controls before making a specificity claim.
This adaptive stop is declared before V1 effects, under the owner's instruction
to omit downstream work made unnecessary by earlier results.

Independently verify unit aggregation, the decision and representative cell
scores directly from raw counts. Hashes bind the configuration, frozen module,
prepared counts, scored cells and summaries. A failed transfer is an outcome of
this instrument and these operational states, not proof that no conserved
regulatory process or fate mechanism exists.
