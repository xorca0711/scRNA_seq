# HPCS annotation eligibility refinement

25 September 2026, before numerical robustness analysis. The two pinned
definition notebooks are 69,091,594 and 49,691,834 bytes, above the existing
20-MiB source-document ceiling. They were not downloaded or executed. The
source inventory preserves this resource hold; it does not establish that
an explicit mapping is absent from the full author code.

Keep every deposited category without learning a biological mapping from
these evaluated cells. Compare all three annotation partitions using the
adjusted Rand index (ARI), invariant to category names, and complete joint
count tables at source and group level. Compare the two cluster fields also
by literal code agreement, explicitly without claiming semantic equivalence.
Retain empty labels as UNASSIGNED. Report per-field category compositions
and leave-one-source-out sensitivity; do not rename a numbered cluster HPCS.
Related partitions are not independent validators. ARI is descriptive here,
with no p-value, accuracy claim, or decision threshold.
