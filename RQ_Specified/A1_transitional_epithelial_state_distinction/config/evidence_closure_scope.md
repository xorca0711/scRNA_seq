# A1 adaptive evidence-closure scope

25 September 2026. The owner requested continuing until the feasible work is
exhausted, adjusting later steps when earlier results make them unnecessary.
This new batch does not overwrite or relax the earlier frozen analyses.

1. Recover the explicit HPCS annotation definitions and inspect exact source
   identities. The previous 20-MiB generic document ceiling prevented reading
   two identified notebooks. For this new acquisition only, permit those pinned
   69.1/49.7-MB notebooks under an 80-MiB per-file, 160-MiB combined ceiling.
   Verify Git blob hashes and extract source text without executing author code.
   The measured cost is bounded and directly addresses the annotation hold.
2. Make one targeted NCBI SRA full-record pass over the already identified
   TIGIT/CD44 experiments. Preserve submitted identities, file names and explicit
   crosswalks; do not infer genotype/pool membership from expression or order.
3. Inspect PATS control/raw-library availability and processing scale. Quantify
   only if matching controls and a common measurement contract can be established.
   Estimate storage/runtime before raw sequencing work; a download alone does
   not fix exposure/sort confounding or create independent replication.
4. Screen narrowly targeted primary sources for an independent regulatory
   comparator, a transition-specificity control, and IRE1α treated/control
   cell-resolved data. Reuse already audited cohorts where appropriate.
   Freeze sample/assay/feature and inferential contracts before new numerical work.
5. Execute newly eligible comparisons; independently verify numerical outputs,
   preserve prior results, and reconsider downstream analysis after each result.
   Do not train a classifier to distinguish studies or use RNA state markers as
   their own independent validation. Bulk deconvolution is unnecessary if a
   suitably labelled cell-resolved dataset directly addresses the same question.
6. Keep a decision ledger for every remaining branch: completed; unnecessary
   after earlier results; or unable to proceed without a precise external input.
   A closed work package is not proof that A1's biological question is solved.

Network budget before any new assay-payload contract: the two pinned notebooks,
24 SRA records (5 MiB each maximum), and small primary articles/catalog metadata
(20 MiB each maximum). No author messages, large raw-read archive, or new paid
service is authorized by this scope. The Notion page remains a concise English
reasoning summary; do not add an analysis menu or critique.
