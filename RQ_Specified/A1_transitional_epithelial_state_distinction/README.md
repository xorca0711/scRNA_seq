# A1 — Transitional epithelial state distinction

Question-specific workspace for [A1 in the canonical register](../../RESEARCH_QUESTIONS.md).

Status: directory established; the expanded analysis specification follows in a
separate change. No new analysis has run as part of this folder migration.

The intended layout is `PLAN.md`, `metadata/`, `config/`, `scripts/`, `tables/`,
`figures/` and `reports/`. Create each output directory when it has real content.
Paper-specific evidence remains in `Research Article/`; shared utilities and
existing cross-question figures remain in `analysis/`. This workspace will link
to them without duplicating their results or replacing the root question register.
